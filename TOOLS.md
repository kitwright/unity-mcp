# KitWright MCP for Unity — Tool Reference

> Auto-generated by `scripts/generate_tools_doc.py` from the `[ToolProvider]` sources.
> Do not hand-edit — run the script after adding, renaming, or removing a tool.

**265 tools across 57 modules.** Tools marked *(core)* are exposed by the default `core` profile; the rest need `full` (or `set_tool_profile`).

## EditorState (18)

| Tool | Description |
|------|-------------|
| `add_layer` | Add a new layer to the first empty user slot (8-31). |
| `add_sorting_layer` | Add a new sorting layer (used to order 2D sprites/renderers). |
| `add_tag` | Add a new tag to TagManager (no-op if it already exists). |
| `get_active_tool` | Get the currently active editor manipulation tool (Move/Rotate/Scale/Rect/Transform/View/None/Custom). |
| `get_build_settings` | Get build settings: enabled scenes (in build order), active build target, scripting backend. |
| `get_editor_state` *(core)* | Get high-level editor runtime state: which project this editor has open, play mode, paused, compiling, updating, time-since-startup, time scale. |
| `get_layers` | List all 32 layer slots with their names (empty for unused slots). |
| `get_prefab_stage` *(core)* | Get info about the currently open prefab stage (if any). |
| `get_selection` *(core)* | Get the current Hierarchy selection: instance ids, names, and paths. |
| `get_sorting_layers` | List all sorting layers (2D render order) with their names. |
| `get_tags` | List every defined tag in TagManager. |
| `get_windows` | List all currently open EditorWindows: title, type, and focused state. |
| `remove_layer` | Remove a user layer (slot 8-31) by name. |
| `remove_sorting_layer` | Remove a sorting layer by name. |
| `remove_tag` | Remove a tag from TagManager. |
| `rename_sorting_layer` | Rename an existing sorting layer. |
| `set_active_tool` | Set the active editor manipulation tool. |
| `set_selection` *(core)* | Replace the current selection. |

## GameObject (14)

| Tool | Description |
|------|-------------|
| `add_component` *(core)* | Add a component to a GameObject. |
| `create_game_object` *(core)* | Create a new empty GameObject in the scene. |
| `create_primitive` *(core)* | Create a primitive GameObject (Cube, Sphere, Capsule, Cylinder, Plane, Quad). |
| `delete_game_object` *(core)* | Delete a GameObject. |
| `duplicate_game_object` | Duplicate a GameObject and optionally rename the copy. |
| `find_game_objects` *(core)* | Find GameObjects by id/name/path/tag/layer/component. |
| `get_game_object_info` *(core)* | Get full info on a GameObject: transform, components (with instance ids), active state, tag, layer. |
| `remove_component` | Remove a component from a GameObject by type name. |
| `rename_game_object` | Rename a GameObject. |
| `set_active` | Activate or deactivate a GameObject. |
| `set_component_enabled` | Enable or disable a component (its 'enabled' checkbox) on a GameObject. |
| `set_parent` *(core)* | Reparent a GameObject. |
| `set_tag_and_layer` | Set tag and/or layer on a GameObject. |
| `set_transform` *(core)* | Set position, rotation, and/or scale on a GameObject's transform. |

## Profiler (13)

| Tool | Description |
|------|-------------|
| `frame_debugger_disable` | Disable the Frame Debugger and close its window. |
| `frame_debugger_enable` | Enable the Frame Debugger by opening a Frame Debugger window and driving its internal capture loop (a bare static SetEnabled call does not populate events without a live window instance). |
| `frame_debugger_get_events` | List frame debugger events for the currently captured frame (requires frame_debugger_enable first, and a moment for the editor to actually render/capture a frame). |
| `get_counters` | Read current values of high-signal rendering/memory counters (Draw Calls, Batches, Triangles, SetPass Calls, GC/Gfx memory, etc). |
| `get_frame_timing` | Get CPU/GPU frame timing for the most recently completed frame(s). |
| `get_object_memory` | Get the runtime memory footprint of a specific asset (by project path) or scene GameObject (by name, hierarchy path e.g. |
| `get_top_memory_objects` | List the top N memory-consuming loaded objects of a given type (Texture2D, RenderTexture, Mesh, AudioClip, Material, AnimationClip, Shader, Sprite, or any UnityEngine.Object-derived type by name), enumerated via Resources.FindObjectsOfTypeAll and measured with Profiler.GetRuntimeMemorySizeLong. |
| `memory_compare_snapshots` | Compare two lightweight memory snapshots (by file name, with or without .json extension) and report the delta for each aggregate category. |
| `memory_list_snapshots` | List previously saved lightweight memory snapshots (see memory_take_snapshot). |
| `memory_take_snapshot` | Take a lightweight memory snapshot (aggregate numbers only, NOT a real Unity .snap file openable in the Memory Profiler window) and save it as JSON under MemoryCaptures/mcp-snapshots/. |
| `profiler_start` | Start a Profiler capture session (enables UnityEngine.Profiling.Profiler and starts a set of persistent ProfilerRecorder counters used by get_counters/get_frame_timing). |
| `profiler_status` | Report whether a Profiler capture session is currently running and how many counters are active. |
| `profiler_stop` | Stop the current Profiler capture session and dispose all persistent ProfilerRecorder counters. |

## Scene (13)

| Tool | Description |
|------|-------------|
| `close_scene` | Close/unload an open scene (used in multi-scene editing). |
| `create_new_scene` | Create a new empty scene. |
| `enter_play_mode` *(core)* | Enter play mode in the editor |
| `exit_play_mode` *(core)* | Exit play mode in the editor |
| `get_scene_info` *(core)* | Get information about every loaded scene (the active scene plus any additively loaded ones), including path, dirty state, and a shallow root-object hierarchy per scene. |
| `list_scenes` | List all scenes in the project |
| `load_scene_additive` | Open a scene additively (keeps currently open scenes loaded), for multi-scene editing. |
| `open_scene` | Open an existing scene by path. |
| `save_scene` *(core)* | Save the current scene. |
| `set_active_scene` | Set which open scene is the active scene (new objects go here; lighting/nav settings follow it). |
| `set_paused` | Pause or resume play mode. |
| `set_time_scale` | Set the game time scale. |
| `step_frame` | Advance play mode by exactly one frame. |

## Prefab (10)

| Tool | Description |
|------|-------------|
| `apply_prefab_overrides` | Apply a prefab instance's overrides back to its source prefab asset, so the changes become part of the prefab itself. |
| `close_prefab_stage` | Close the currently open prefab stage and return to the main stage. |
| `create_prefab` | Create a prefab from a GameObject in the scene |
| `create_prefab_variant` | Create a prefab variant from an existing prefab asset. |
| `get_prefab_variant_info` | Get prefab info for a scene instance or prefab asset: prefab type (Regular/Variant/Model), asset path, whether it is a variant, and its base prefab path. |
| `instantiate_prefab` | Instantiate a prefab in the scene |
| `open_prefab_stage` | Open a prefab asset in Prefab Mode (an isolated prefab stage) for editing its contents directly, without instantiating it into a scene. |
| `revert_prefab_overrides` | Revert a prefab instance's overrides, restoring it to match its source prefab asset. |
| `save_prefab_stage` | Save the currently open prefab stage back to its .prefab asset, without closing the stage. |
| `unpack_prefab` | Unpack a prefab instance in the scene |

## Asset (8)

| Tool | Description |
|------|-------------|
| `assign_material` | Assign a material to a GameObject's renderer |
| `copy_asset` | Copy an asset to a new location |
| `create_folder` | Create a project folder (and any missing parent folders) under Assets. |
| `create_material` | Create a new material with a specified color |
| `delete_asset` | Delete an asset by moving it to the OS trash (Recycle Bin). |
| `find_assets` | Search for assets by type and name |
| `move_asset` | Move or rename an asset to a new path (creates missing destination folders). |
| `rename_asset` | Rename an asset |

## Terrain (8)

| Tool | Description |
|------|-------------|
| `add_terrain_layer` | Add a TerrainLayer (texture) to a terrain. |
| `add_tree_prototype` | Register a tree prefab as a tree prototype on the terrain. |
| `adjust_terrain_height` | Raise or lower the whole terrain by a delta height in world units (positive raises, negative lowers). |
| `create_terrain` | Create a Terrain GameObject with a fresh TerrainData. |
| `flatten_terrain` | Flatten the entire terrain to a uniform height. |
| `get_terrain_info` | Get info about a terrain: size, heightmap resolution, terrain layers, tree/detail prototype counts. |
| `paint_terrain_layer` | Paint a single terrain layer across the entire terrain at full opacity (fills the splatmap with that layer). |
| `place_terrain_trees` | Scatter tree instances randomly across the terrain using a registered tree prototype. |

## AssemblyDefinition (7)

| Tool | Description |
|------|-------------|
| `add_assembly_references` | Add assembly references to an existing .asmdef. |
| `create_assembly_def` | Create a new Assembly Definition (.asmdef) file. |
| `get_assembly_def_info` | Get detailed info about an .asmdef file: name, references, platforms, and flags. |
| `list_assembly_defs` | List all Assembly Definition files, optionally scoped to a folder and including Packages/. |
| `remove_assembly_references` | Remove assembly references from an existing .asmdef. |
| `set_assembly_platforms` | Set include/exclude platform lists on an .asmdef. |
| `update_assembly_def_settings` | Update settings on an .asmdef: name, rootNamespace, allowUnsafeCode, autoReferenced, noEngineReferences, defineConstraints. |

## Prefs (7)

| Tool | Description |
|------|-------------|
| `delete_all_player_prefs` | Delete ALL PlayerPrefs keys for this project. |
| `delete_editor_pref` | Delete an EditorPrefs key. |
| `delete_player_pref` | Delete a PlayerPrefs key. |
| `get_editor_pref` | Read an EditorPrefs value (editor-only, persists across sessions on this machine). |
| `get_player_pref` | Read a PlayerPrefs value (shipped with the game, persists across runs). |
| `set_editor_pref` | Write an EditorPrefs value (editor-only, persists across sessions on this machine). |
| `set_player_pref` | Write a PlayerPrefs value (shipped with the game, persists across runs). |

## SpriteAtlas (7)

| Tool | Description |
|------|-------------|
| `add_to_sprite_atlas` | Add sprites, textures, or folders (as packables) to a SpriteAtlas. |
| `create_sprite_atlas` | Create a new SpriteAtlas asset (.spriteatlas) at the given path. |
| `delete_sprite_atlas` | Delete a SpriteAtlas asset by moving it to the OS trash (Recycle Bin). |
| `get_sprite_atlas_info` | Get SpriteAtlas details: packables, sprite count, include-in-build, packing/texture settings. |
| `list_sprite_atlases` | List all SpriteAtlas assets in the project, optionally scoped to a folder. |
| `remove_from_sprite_atlas` | Remove packables from a SpriteAtlas. |
| `set_sprite_atlas_settings` | Update SpriteAtlas packing and texture settings. |

## Visual (7)

| Tool | Description |
|------|-------------|
| `clear_console` | Clear the Unity Editor console. |
| `focus_on_object` | Focus the Scene View camera on a specific GameObject |
| `get_console_logs` *(core)* | Get recent console log messages from Unity. |
| `log_message` | Log a message to the Unity console |
| `ping_asset` | Ping/highlight an asset in the Project window |
| `select_object` | Select a GameObject in the scene hierarchy and inspector |
| `show_dialog` | Show a message to the user as a non-blocking notification in the Scene View (also written to the console) |

## Addressable (6)

| Tool | Description |
|------|-------------|
| `get_addressable_info` | Get an asset's Addressable info: whether it's addressable, its address, group, and labels. |
| `list_addressable_groups` | List all Addressable groups and their entry counts. |
| `mark_addressable` | Mark an asset as Addressable and set its address key. |
| `set_addressable_address` | Set the address key of an already-addressable asset. |
| `set_addressable_label` | Add or remove a label on an addressable asset. |
| `unmark_addressable` | Remove an asset's Addressable entry (unmark it). |

## Animation (6)

| Tool | Description |
|------|-------------|
| `assign_animator` | Assign an Animator Controller to a GameObject |
| `create_animation_clip` | Create an Animation Clip asset |
| `create_animator_controller` | Create an Animator Controller asset |
| `get_animator_state` | Get an Animator's current runtime state: active state name (resolved from the controller when possible), normalized time, transition status, and all parameters with their current values. |
| `play_animator_state` | Play a specific Animator state by name, e.g. |
| `set_animator_parameter` | Set an Animator parameter by name. |

## Audio (6)

| Tool | Description |
|------|-------------|
| `add_audio_listener` | Add an AudioListener to a GameObject (usually the main camera). |
| `add_audio_source` | Add and configure an AudioSource on a GameObject. |
| `get_audio_source_info` | Get the AudioSource settings on a GameObject. |
| `play_clip_preview` | Preview-play an AudioClip in the editor without entering Play Mode (uses the internal AudioUtil API). |
| `set_global_audio` | Set global audio settings: master volume (AudioListener.volume) and/or pause all audio (AudioListener.pause). |
| `stop_clip_preview` | Stop all editor audio clip previews started by play_clip_preview. |

## Code (6)

| Tool | Description |
|------|-------------|
| `create_script` | Create a new C# script with the specified content |
| `edit_script` | Replace the entire contents of an existing script. |
| `edit_script_members` | Edit a C# script one member at a time, addressing methods by name instead of by text. |
| `get_script_sha` | Get the SHA256 hash of a script file's current contents. |
| `patch_script` | Patch a script by finding and replacing specific text. |
| `validate_script` | Check that a C# file is structurally sound — balanced braces, parentheses and brackets, no unterminated string or comment — reporting the line of the first problem. |

## InputActions (6)

| Tool | Description |
|------|-------------|
| `add_input_action` | Add an action (like 'Jump' or 'Move') to an action map. |
| `add_input_binding` | Add a simple binding (single control path like '<Keyboard>/space' or '<Gamepad>/buttonSouth') to an action. |
| `add_input_composite_binding` | Add a composite binding (combines several controls into one, like WASD into a 2D vector) to an action. |
| `add_input_map` | Add an action map (group of related actions like 'Player' or 'UI') to an existing .inputactions asset. |
| `create_input_actions` | Create a new empty Input Actions asset (.inputactions) for the New Input System. |
| `get_input_actions_info` | List the structure of an .inputactions asset: action maps, their actions (with type), and each action's binding count. |

## NavMesh (6)

| Tool | Description |
|------|-------------|
| `add_nav_mesh_agent` | Add a NavMeshAgent component to a GameObject so it can path-find on a baked NavMesh. |
| `add_nav_mesh_obstacle` | Add a NavMeshObstacle component to a GameObject so it blocks or carves the NavMesh. |
| `bake_nav_mesh` | Bake the NavMesh for the current scene using the legacy scene NavMesh settings. |
| `clear_nav_mesh` | Clear all baked NavMesh data from the current scene. |
| `get_nav_mesh_info` | Get NavMesh info for the current scene: baked vertex and triangle counts, and per-area default costs. |
| `set_agent_destination` | Set a NavMeshAgent's destination so it starts path-finding. |

## Shader (6)

| Tool | Description |
|------|-------------|
| `create_shader` | Create a new .shader source file in the project. |
| `delete_shader` | Delete a .shader file by moving it to the OS trash (Recycle Bin). |
| `get_shader_info` | Get metadata for a shader loaded in the project (by shader name as declared in the Shader "..." line): supported flag, render queue, LOD, and its exposed properties with types. |
| `list_shaders` | List shader files (.shader) in the project. |
| `read_shader` | Read the source of a .shader file in the project. |
| `update_shader` | Overwrite the source of an existing .shader file. |

## UI (6)

| Tool | Description |
|------|-------------|
| `create_button` | Create a UI Button |
| `create_canvas` | Create a Canvas in the scene (required for UI elements) |
| `create_image` | Create a UI Image element |
| `create_text` | Create a UI Text element |
| `raycast_at_point` *(core)* | Diagnose what UI (or physics) elements a click/tap at a screen point would hit, using the live EventSystem's RaycastAll. |
| `set_rect_transform` | Set RectTransform layout on a UI element: anchor preset, anchored position, size, and pivot. |

## File (5)

| Tool | Description |
|------|-------------|
| `exists` | Check if a file or directory exists |
| `list_directory` | List files and directories directly inside a directory (top level only; use search_files to recurse into subdirectories) |
| `read_file` | Read the contents of a file. |
| `search_files` | Search for files by name pattern in the project |
| `write_file` | Write content to a file, creating it or overwriting it whole. |

## MemorySnapshot (5)

| Tool | Description |
|------|-------------|
| `memory_list_full_snapshots` | List the REAL .snap memory snapshots in the Memory Profiler package's snapshot folder (the ones memory_take_full_snapshot writes and the Memory Profiler window lists). |
| `memory_open_snapshot_in_profiler` | Open a REAL .snap memory snapshot in the Memory Profiler package window for object-level reference-chain analysis. |
| `memory_query_references` | Query a REAL .snap memory snapshot for what references a specific native object, or what it references -- structured JSON reference-chain data, no window or screenshot needed. |
| `memory_query_top_objects` | Query a REAL .snap memory snapshot for the largest native objects (Texture2D, Mesh, RenderTexture, AudioClip, etc.) -- structured JSON, no window or screenshot needed. |
| `memory_take_full_snapshot` | Capture a REAL Unity memory snapshot (.snap) -- the full-detail file the Memory Profiler package opens for object-level reference-chain analysis ('who is holding this texture alive'). |

## SceneView (5)

| Tool | Description |
|------|-------------|
| `align_view_to_object` | Align the Scene View camera to match a camera or GameObject's position and orientation (like GameObject > Align View to Selected). |
| `frame_object` | Frame the Scene View camera on a GameObject (like pressing F with it selected), so it fills the view. |
| `get_scene_view_camera` | Get the current Scene View camera state: pivot, rotation, size, orthographic, camera world position. |
| `look_at_point` | Point the Scene View camera at a world position without changing distance much. |
| `set_scene_view_camera` | Manually set the Scene View camera pivot, rotation (euler degrees), and orbit size (distance). |

## Volume (5)

| Tool | Description |
|------|-------------|
| `add_volume_override` | Add a post-processing override to a Volume profile (URP). |
| `create_volume` | Create a post-processing Volume GameObject (URP). |
| `get_volume_info` | List post-processing overrides on a Volume's profile (URP), with each override's active state. |
| `remove_volume_override` | Remove a post-processing override from a Volume profile (URP). |
| `set_volume_override_property` | Set a single parameter on a Volume override and enable its overrideState (URP). |

## Camera (4)

| Tool | Description |
|------|-------------|
| `get_camera_properties` | Get all camera properties including type, FOV, clipping planes, background, and culling mask |
| `set_camera_culling_mask` | Set the camera culling mask to show/hide specific layers |
| `set_camera_projection` | Set camera projection to orthographic or perspective with size/FOV |
| `set_camera_settings` | Set camera clipping planes, background color, and clear flags |

## Compilation (4)

| Tool | Description |
|------|-------------|
| `get_compilation_errors` *(core)* | Get the latest Unity script compilation errors from the most recent compilation cycle. |
| `get_reload_recovery_status` *(core)* | Get the latest domain reload recovery event, if any. |
| `request_recompile` *(core)* | IMPORTANT: This should be the AI's default next step immediately after editing project files outside the Unity Editor. |
| `wait_for_compilation` *(core)* | Force Unity to refresh and wait until script compilation is complete without blocking the editor thread. |

## ComponentProperty (4)

| Tool | Description |
|------|-------------|
| `get_component_properties` *(core)* | Get all serialized properties on a component, including [SerializeField] private fields. |
| `list_components` *(core)* | List components on a GameObject. |
| `set_component_properties` *(core)* | Set multiple properties on a component in one call. |
| `set_component_property` *(core)* | Set a single property or field on a component. |

## InputSimulation (4)

| Tool | Description |
|------|-------------|
| `simulate_key_combo` *(core)* | Simulate multiple keys held simultaneously in Play Mode, for example W plus LeftShift. |
| `simulate_key_press` *(core)* | Simulate keyboard input in Play Mode. |
| `simulate_mouse_click` *(core)* | Simulate a mouse click at a screen position in Play Mode. |
| `simulate_mouse_drag` *(core)* | Simulate a mouse drag from one screen position to another in Play Mode using the Unity Input System. |

## LodConstraint (4)

| Tool | Description |
|------|-------------|
| `add_constraint` | Add an animation constraint (position, rotation, scale, aim, lookat, parent) to a GameObject, optionally binding a source object and activating it. |
| `create_lod_group` | Create or reconfigure a LODGroup on a GameObject with N evenly-spaced LOD levels. |
| `get_constraint_info` | List all animation constraints on a GameObject with type, active/locked state, weight, and source count. |
| `get_lod_group_info` | Get LODGroup info on a GameObject: level count, per-level transition height and renderer count. |

## Screenshot (4)

| Tool | Description |
|------|-------------|
| `capture_editor_window` *(core)* | Capture a screenshot of any open EditorWindow (Inspector, Console, Project, custom tool windows...) identified by its title or type name. |
| `capture_game_view` *(core)* | Capture a screenshot of the Game View (what the main camera sees). |
| `capture_scene_view` *(core)* | Capture a screenshot of the Scene View (the editor's scene camera perspective). |
| `capture_simulator_view` | Capture the Unity Device Simulator screen. |

## Script (4)

| Tool | Description |
|------|-------------|
| `clear_execute_code_history` | Erase the entire execute_code history for the current Editor session. |
| `execute_code` *(core)* | Primary high-flexibility execution tool. |
| `get_execute_code_history` | Return the most recent execute_code invocations (success or failure) from the current Editor session. |
| `replay_execute_code` | Re-run a past execute_code invocation by index (use get_execute_code_history to discover indices). |

## Texture (4)

| Tool | Description |
|------|-------------|
| `apply_gradient` | Create a gradient .png texture. |
| `apply_noise` | Create a Perlin-noise .png texture, mapping the noise value through the palette. |
| `apply_pattern` | Create a procedural pattern .png texture. |
| `create_texture` | Create a solid-color .png texture asset. |

## Build (3)

| Tool | Description |
|------|-------------|
| `build_player` | Build a standalone/player build with Unity's BuildPipeline. |
| `modify_build_scenes` | Add, remove, or set the enabled/disabled state of a scene in the Build Settings scene list. |
| `switch_build_target` | Switch the active build target platform (triggers a platform switch + recompile). |

## ComponentBatch (3)

| Tool | Description |
|------|-------------|
| `add_component_to_many` | Add a component of component_type to many GameObjects in one Undo-tracked call. |
| `copy_component` | Copy a component from a source GameObject into the editor's SHARED component clipboard so its values can be pasted onto other objects with paste_component_values. |
| `paste_component_values` | Paste the editor's SHARED component clipboard (last filled by copy_component) onto many GameObjects. |

## Docs (3)

| Tool | Description |
|------|-------------|
| `fetch_docs` *(core)* | Fetch Unity documentation pages and return them as plain text, so usage notes and code examples arrive without a separate web fetch. |
| `get_script_reference_url` | Get the Unity documentation URL for a scripting API type or member. |
| `search_manual` | Get a Unity Manual search URL for a topic (e.g. |

## Lighting (3)

| Tool | Description |
|------|-------------|
| `bake_lightmaps` | Start an asynchronous lightmap bake of the current scene. |
| `get_lighting_settings` | Read the active scene's environment lighting: ambient (mode/light/intensity), skybox material, fog (enabled/mode/color/density), the active LightingSettings asset (if any), and whether a lightmap bake is running. |
| `set_lighting_settings` | Set scene environment lighting. |

## Package (3)

| Tool | Description |
|------|-------------|
| `install_package` | Install a Unity package by name |
| `list_packages` | List all installed packages |
| `remove_package` | Remove a Unity package |

## Physics (3)

| Tool | Description |
|------|-------------|
| `physics2d_overlap_point` | 2D overlap-point query (Physics2D.OverlapPointAll) for the 2D merge board. |
| `physics_overlap` | Overlap query against 3D scene colliders using Physics.OverlapSphere (shape=sphere) or Physics.OverlapBox (shape=box). |
| `physics_raycast` | Cast a 3D ray against scene colliders (Physics.RaycastAll) and return every hit ordered nearest-first. |

## ScriptableObject (3)

| Tool | Description |
|------|-------------|
| `create_scriptable_object` | Create a new ScriptableObject asset of the given type at the given path. |
| `get_scriptable_object` | Get all serialized properties of a ScriptableObject asset, including [SerializeField] private fields. |
| `set_scriptable_object_properties` | Set one or more serialized properties on a ScriptableObject asset and save it. |

## Sprite (3)

| Tool | Description |
|------|-------------|
| `get_sprite_sheet_info` | List the sub-sprites defined on a Multiple-mode sprite sheet: name and pixel rect. |
| `set_texture_as_sprite` | Set a texture asset's type to Sprite. |
| `slice_sprite_grid` | Slice a Sprite sheet into a uniform grid of cells. |

## Testing (3)

| Tool | Description |
|------|-------------|
| `cancel_test_run` | Cancel a running test run. |
| `get_test_job` | Get the status and results of a test run started by run_tests. |
| `run_tests` | Run Unity Test Runner tests (EditMode or PlayMode) asynchronously. |

## Undo (3)

| Tool | Description |
|------|-------------|
| `get_undo_state` | Read the current state of the Editor's undo stack (top group name and index). |
| `redo` | Perform a single Redo step. |
| `undo` | Perform a single Undo step. |

## AssetImport (2)

| Tool | Description |
|------|-------------|
| `get_asset_import_settings` | Read the import settings of an asset via its AssetImporter. |
| `set_asset_import_settings` | Apply import settings to an asset. |

## EditorDialog (2)

| Tool | Description |
|------|-------------|
| `dismiss_editor_dialog` | Click a button on the modal dialog blocking the editor, to unstick it without leaving the chair. |
| `get_editor_dialog` | Report the modal dialog currently blocking the Unity editor: its title, message and button captions. |

## EditorWindowInteraction (2)

| Tool | Description |
|------|-------------|
| `simulate_editor_window_click` | Click inside any open EditorWindow (Inspector, Console, Project, custom tool windows...) as a real user would. |
| `simulate_editor_window_key` | Type text or send a key into any open EditorWindow as a real user would (e.g. |

## Material (2)

| Tool | Description |
|------|-------------|
| `get_material_properties` | Inspect a Material: shader name, enabled keywords, render queue, and every shader property with its type and current value. |
| `set_material_property` | Set a single shader property on a Material and save the asset. |

## MenuItem (2)

| Tool | Description |
|------|-------------|
| `execute_menu_item` *(core)* | Execute an editor menu item by its full path. |
| `reset_learned_modal_menu_items` | List, and optionally clear, the menu paths this project learned open a modal dialog. |

## Performance (2)

| Tool | Description |
|------|-------------|
| `analyze_scene_complexity` *(core)* | Analyze the current scene for common performance hotspots. |
| `get_performance_snapshot` *(core)* | Get a lightweight performance snapshot of the current Unity session. |

## References (2)

| Tool | Description |
|------|-------------|
| `find_broken_references` | Scan for broken references: (a) missing MonoBehaviour scripts (a null Component entry) and (b) dangling Object references (a serialized ObjectReference whose instanceID is non-zero but whose resolved value is null). |
| `find_references` | Find asset references in both directions. |

## Batch (1)

| Tool | Description |
|------|-------------|
| `batch_execute` | Run multiple MCP tool calls sequentially in a single request, on the main thread, saving round-trips. |

## Hierarchy (1)

| Tool | Description |
|------|-------------|
| `get_hierarchy` *(core)* | Browse the scene hierarchy tree. |

## Interop (1)

| Tool | Description |
|------|-------------|
| `get_code_patching_status` | Report the status of known third-party code-patching plugins (currently: SingularityGroup Hot Reload). |

## Mesh (1)

| Tool | Description |
|------|-------------|
| `get_mesh_info` | Inspect a Mesh. |

## Particle (1)

| Tool | Description |
|------|-------------|
| `particle_control` | Control a ParticleSystem's playback for deterministic VFX inspection/screenshots. |

## ProjectSettings (1)

| Tool | Description |
|------|-------------|
| `get_project_settings` | Read project settings. |

## Reflection (1)

| Tool | Description |
|------|-------------|
| `reflect_api` *(core)* | Inspect the live C# API by reflection — verify a type and its members exist before writing an execute_code snippet. |

## Timeline (1)

| Tool | Description |
|------|-------------|
| `director_evaluate` | Set a PlayableDirector's playback time and Evaluate() it — samples the bound Timeline at that time in the editor. |

## ToolExposure (1)

| Tool | Description |
|------|-------------|
| `set_tool_profile` *(core)* | Switch the MCP tool profile exposed to clients: 'minimal', 'core', 'extended' or 'full'. |
